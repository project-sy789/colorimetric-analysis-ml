# Design Document

## Overview

แอปพลิเคชัน Colorimetric Analysis เป็นเว็บแอปพลิเคชันที่พัฒนาด้วย Streamlit สำหรับการวิเคราะห์ค่าความเข้มข้นของสารจากค่าสี RGB ในรูปภาพ แอปพลิเคชันใช้ Machine Learning (Regression) ในการเรียนรู้ความสัมพันธ์ระหว่างค่าสีกับค่าความเข้มข้น และทำนายค่าความเข้มข้นจากภาพใหม่

แอปพลิเคชันประกอบด้วย 3 โมดูลหลัก:
1. **Data Collection Module**: รวบรวมข้อมูล RGB และค่าความเข้มข้นจากภาพ
2. **Model Training Module**: เทรนโมเดล ML จากข้อมูลที่รวบรวม
3. **Prediction Module**: ทำนายค่าความเข้มข้นจากภาพใหม่

## Architecture

### High-Level Architecture

```
┌─────────────────────────────────────────────────────────┐
│                  Streamlit Web Interface                │
├─────────────────┬─────────────────┬─────────────────────┤
│  Data Collection│  Model Training │    Prediction       │
│     Module      │     Module      │      Module         │
└────────┬────────┴────────┬────────┴──────────┬──────────┘
         │                 │                   │
         ▼                 ▼                   ▼
    ┌─────────┐      ┌──────────┐       ┌──────────┐
    │ Image   │      │ Dataset  │       │  Model   │
    │Processor│      │ Manager  │       │ Manager  │
    └────┬────┘      └─────┬────┘       └─────┬────┘
         │                 │                   │
         ▼                 ▼                   ▼
    ┌─────────────────────────────────────────────┐
    │           File System Storage               │
    │  - dataset.csv                              │
    │  - model.joblib                             │
    └─────────────────────────────────────────────┘
```

### Technology Stack

- **Frontend Framework**: Streamlit
- **Image Processing**: OpenCV (cv2), PIL/Pillow
- **Data Management**: Pandas
- **Machine Learning**: Scikit-learn (RandomForestRegressor)
- **Model Persistence**: Joblib
- **Programming Language**: Python 3.8+

## Components and Interfaces

### 1. Main Application Component (`app.py`)

**Responsibility**: จัดการ UI หลักและการนำทางระหว่างโมดูล

**Key Functions**:
- `main()`: ฟังก์ชันหลักที่รัน Streamlit app
- จัดการ tab navigation หรือ selectbox สำหรับสลับระหว่างโมดูล
- เรียกใช้ฟังก์ชันของแต่ละโมดูลตามที่ผู้ใช้เลือก

**Interface**:
```python
def main():
    st.title("Colorimetric Analysis Application")
    st.write("แอปพลิเคชันวิเคราะห์ค่าความเข้มข้นจากค่าสี RGB")
    
    # Navigation
    tab1, tab2, tab3 = st.tabs(["รวบรวมข้อมูล", "เทรนโมเดล", "ทำนายผล"])
    
    with tab1:
        data_collection_module()
    with tab2:
        model_training_module()
    with tab3:
        prediction_module()
```

### 2. Image Processor Component

**Responsibility**: ประมวลผลภาพและคำนวณค่า RGB เฉลี่ย

**Key Functions**:
- `extract_rgb_from_image(image_file, roi_size=100)`: คำนวณค่า RGB เฉลี่ยจาก ROI
  - Input: image file (uploaded file object), roi_size (ขนาด ROI)
  - Output: tuple (R, G, B) ค่าเฉลี่ยของแต่ละช่องสี
  - Process:
    1. อ่านภาพด้วย OpenCV
    2. แปลงจาก BGR เป็น RGB
    3. หาจุดกึ่งกลางของภาพ
    4. ตัดเอา ROI ขนาด 100x100 จากจุดกึ่งกลาง (หรือใช้ภาพทั้งหมดถ้าเล็กกว่า)
    5. คำนวณค่าเฉลี่ยของแต่ละช่องสี

**Interface**:
```python
def extract_rgb_from_image(image_file, roi_size=100):
    """
    Extract average RGB values from the center region of an image.
    
    Args:
        image_file: Uploaded image file object
        roi_size: Size of the ROI (default 100x100 pixels)
    
    Returns:
        tuple: (R, G, B) average values
    """
    # Implementation details in code
    pass
```

### 3. Data Collection Module

**Responsibility**: จัดการ UI และ logic สำหรับการรวบรวมข้อมูล

**Key Functions**:
- `data_collection_module()`: แสดง UI และจัดการการบันทึกข้อมูล
  - แสดง file uploader
  - แสดงภาพที่อัปโหลด
  - คำนวณและแสดงค่า RGB
  - รับค่าความเข้มข้นจากผู้ใช้
  - บันทึกข้อมูลลง CSV

- `save_data_to_csv(r, g, b, concentration, filename='dataset.csv')`: บันทึกข้อมูลลงไฟล์
  - Input: R, G, B values, concentration, filename
  - Output: None (บันทึกลงไฟล์)
  - Process:
    1. ตรวจสอบว่าไฟล์มีอยู่หรือไม่
    2. ถ้าไม่มี สร้างไฟล์ใหม่พร้อม header
    3. append ข้อมูลใหม่ลงไฟล์

**Interface**:
```python
def data_collection_module():
    st.header("📊 รวบรวมข้อมูล")
    
    uploaded_file = st.file_uploader("อัปโหลดรูปภาพ", type=['png', 'jpg', 'jpeg'])
    
    if uploaded_file:
        # Display image
        # Extract RGB
        # Show RGB values
        # Input concentration
        # Save button
        pass

def save_data_to_csv(r, g, b, concentration, filename='dataset.csv'):
    # Implementation
    pass
```

### 4. Model Training Module

**Responsibility**: จัดการการเทรนโมเดล ML และบันทึกโมเดล

**Key Functions**:
- `model_training_module()`: แสดง UI และจัดการการเทรนโมเดล
  - แสดงปุ่มเริ่มเทรน
  - โหลดข้อมูลจาก CSV
  - เทรนโมเดล
  - แสดงผลความแม่นยำ
  - บันทึกโมเดล

- `train_model(dataset_file='dataset.csv', model_file='model.joblib')`: เทรนและบันทึกโมเดล
  - Input: dataset filename, model filename
  - Output: trained model, R² score
  - Process:
    1. โหลดข้อมูลจาก CSV ด้วย Pandas
    2. แยก features (R, G, B) และ target (Concentration)
    3. สร้างและเทรน RandomForestRegressor
    4. คำนวณ R² score
    5. บันทึกโมเดลด้วย joblib

**Interface**:
```python
def model_training_module():
    st.header("🤖 เทรนโมเดล")
    
    if st.button("เริ่มเทรนโมเดล"):
        # Train model
        # Show results
        pass

def train_model(dataset_file='dataset.csv', model_file='model.joblib'):
    """
    Train a regression model and save it.
    
    Returns:
        tuple: (model, r2_score)
    """
    # Implementation
    pass
```

### 5. Prediction Module

**Responsibility**: จัดการการทำนายค่าความเข้มข้นจากภาพใหม่

**Key Functions**:
- `prediction_module()`: แสดง UI และจัดการการทำนาย
  - แสดง file uploader
  - โหลดโมเดล
  - คำนวณ RGB จากภาพใหม่
  - ทำนายค่าความเข้มข้น
  - แสดงผลลัพธ์

- `load_model(model_file='model.joblib')`: โหลดโมเดลที่บันทึกไว้
  - Input: model filename
  - Output: trained model object
  - Raises: FileNotFoundError ถ้าไม่พบไฟล์

**Interface**:
```python
def prediction_module():
    st.header("🔮 ทำนายผล")
    
    uploaded_file = st.file_uploader("อัปโหลดรูปภาพใหม่", type=['png', 'jpg', 'jpeg'], key='predict')
    
    if uploaded_file:
        # Load model
        # Extract RGB
        # Predict
        # Show results
        pass

def load_model(model_file='model.joblib'):
    # Implementation
    pass
```

## Data Models

### Dataset CSV Structure

```csv
R,G,B,Concentration
120.5,85.3,45.2,10.5
115.2,80.1,42.8,9.8
...
```

**Columns**:
- `R`: ค่าเฉลี่ยของช่องสีแดง (0-255)
- `G`: ค่าเฉลี่ยของช่องสีเขียว (0-255)
- `B`: ค่าเฉลี่ยของช่องสีน้ำเงิน (0-255)
- `Concentration`: ค่าความเข้มข้นของสาร (mg/L)

### Model Object

- **Type**: `sklearn.ensemble.RandomForestRegressor`
- **Input Features**: 3 features (R, G, B)
- **Output**: 1 target (Concentration)
- **Hyperparameters**: 
  - `n_estimators=100` (จำนวน trees)
  - `random_state=42` (สำหรับ reproducibility)

## Error Handling

### Data Collection Module

1. **Invalid Image Format**:
   - Check: ตรวจสอบนามสกุลไฟล์
   - Action: แสดง error message "รูปแบบไฟล์ไม่ถูกต้อง กรุณาอัปโหลด PNG, JPG หรือ JPEG"

2. **Image Processing Error**:
   - Check: try-except block รอบการประมวลผลภาพ
   - Action: แสดง error message พร้อมรายละเอียด

3. **Missing Concentration Value**:
   - Check: ตรวจสอบว่าผู้ใช้กรอกค่าหรือไม่
   - Action: แสดง warning "กรุณากรอกค่าความเข้มข้น"

### Model Training Module

1. **Dataset File Not Found**:
   - Check: ตรวจสอบว่าไฟล์ dataset.csv มีอยู่หรือไม่
   - Action: แสดง error "ไม่พบไฟล์ข้อมูล กรุณารวบรวมข้อมูลก่อน"

2. **Insufficient Data**:
   - Check: ตรวจสอบจำนวนแถวในข้อมูล (ควรมีอย่างน้อย 5 แถว)
   - Action: แสดง warning "ข้อมูลไม่เพียงพอ กรุณารวบรวมข้อมูลเพิ่มเติม"

3. **Training Error**:
   - Check: try-except block รอบการเทรน
   - Action: แสดง error message พร้อมรายละเอียด

### Prediction Module

1. **Model File Not Found**:
   - Check: ตรวจสอบว่าไฟล์ model.joblib มีอยู่หรือไม่
   - Action: แสดง error "ไม่พบโมเดล กรุณาเทรนโมเดลก่อน"

2. **Image Processing Error**:
   - Check: try-except block รอบการประมวลผลภาพ
   - Action: แสดง error message พร้อมรายละเอียด

3. **Prediction Error**:
   - Check: try-except block รอบการทำนาย
   - Action: แสดง error message พร้อมรายละเอียด

## Testing Strategy

### Unit Testing

1. **Image Processing Functions**:
   - Test `extract_rgb_from_image()` กับภาพขนาดต่างๆ
   - Test การจัดการภาพที่เล็กกว่า ROI size
   - Test การแปลงสีจาก BGR เป็น RGB

2. **Data Management Functions**:
   - Test `save_data_to_csv()` การสร้างไฟล์ใหม่
   - Test การ append ข้อมูลลงไฟล์ที่มีอยู่
   - Test การอ่านข้อมูลจาก CSV

3. **Model Functions**:
   - Test `train_model()` กับข้อมูลตัวอย่าง
   - Test `load_model()` การโหลดโมเดลที่บันทึกไว้
   - Test การทำนายด้วยโมเดล

### Integration Testing

1. **End-to-End Workflow**:
   - Test การรวบรวมข้อมูล → เทรนโมเดล → ทำนายผล
   - Test การบันทึกและโหลดไฟล์ทั้งหมด

2. **UI Testing**:
   - Test การอัปโหลดไฟล์ผ่าน Streamlit
   - Test การแสดงผลและ interaction ของแต่ละโมดูล

### Manual Testing

1. **User Acceptance Testing**:
   - ทดสอบกับรูปภาพจริงจากการทดลอง colorimetric
   - ตรวจสอบความแม่นยำของการทำนาย
   - ทดสอบ usability ของ UI

## File Structure

```
colorimetric-analysis-app/
├── app.py                 # Main Streamlit application
├── requirements.txt       # Python dependencies
├── dataset.csv           # Generated: Training data
├── model.joblib          # Generated: Trained model
└── README.md             # Documentation (optional)
```

## Dependencies

```
streamlit>=1.28.0
pandas>=2.0.0
opencv-python>=4.8.0
scikit-learn>=1.3.0
joblib>=1.3.0
pillow>=10.0.0
numpy>=1.24.0
```

## Design Decisions and Rationales

### 1. Choice of RandomForestRegressor

**Decision**: ใช้ RandomForestRegressor แทน LinearRegression

**Rationale**:
- RandomForest สามารถจับความสัมพันธ์ที่ไม่เป็นเชิงเส้นได้ดีกว่า
- มีความทนทานต่อ outliers
- ไม่ต้อง feature scaling
- ให้ผลลัพธ์ที่ดีกับข้อมูลขนาดเล็กถึงกลาง

### 2. ROI Selection Method

**Decision**: ใช้ ROI ขนาด 100x100 พิกเซลจากจุดกึ่งกลางภาพ

**Rationale**:
- ง่ายต่อการ implement และใช้งาน
- เหมาะกับการถ่ายภาพที่วางตัวอย่างไว้ตรงกลาง
- สามารถขยายเป็น interactive ROI selection ได้ในอนาคต

### 3. Data Storage Format

**Decision**: ใช้ CSV สำหรับเก็บข้อมูล

**Rationale**:
- อ่านและเขียนง่ายด้วย Pandas
- Human-readable สามารถตรวจสอบข้อมูลได้ง่าย
- เหมาะกับข้อมูลขนาดเล็กถึงกลาง
- สามารถ export ไปใช้กับเครื่องมืออื่นได้

### 4. Model Persistence

**Decision**: ใช้ Joblib สำหรับบันทึกโมเดล

**Rationale**:
- เป็น standard สำหรับ scikit-learn models
- มีประสิทธิภาพในการบันทึกและโหลด
- รองรับ compression
- เข้ากันได้ดีกับ numpy arrays

### 5. UI Framework

**Decision**: ใช้ Streamlit

**Rationale**:
- เขียนได้ง่ายด้วย Python อย่างเดียว
- มี built-in components สำหรับ file upload, display images
- รองรับ interactive widgets
- เหมาะกับการสร้าง ML/Data Science applications
- Deploy ได้ง่าย
