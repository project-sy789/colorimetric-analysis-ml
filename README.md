# 🔬 Colorimetric Analysis Application

แอปพลิเคชันวิเคราะห์ค่าความเข้มข้นของสารจากค่าสี RGB ในรูปภาพ โดยใช้ Machine Learning

## 📋 คำอธิบาย

แอปพลิเคชันนี้เป็นเว็บแอปพลิเคชันที่พัฒนาด้วย Python และ Streamlit สำหรับการวิเคราะห์เชิงสี (Colorimetric Analysis) เพื่อเทรนโมเดล Machine Learning ในการทำนายค่าความเข้มข้นของสาร (เช่น ฟอสเฟต) จากค่าสี RGB ที่วิเคราะห์จากรูปภาพ

### ✨ ฟีเจอร์หลัก

- **🧪 Multi-Profile Support**: รองรับการวิเคราะห์สารหลายชนิด (Phosphate, Nitrate, Ammonia, ฯลฯ) แต่ละ profile มี dataset และ model แยกกัน
- **📊 รวบรวมข้อมูล**: อัปโหลดรูปภาพและบันทึกค่า RGB พร้อมค่าความเข้มข้นจริง พร้อมบันทึกชื่อไฟล์รูป
- **🎯 ปรับ ROI แบบ Manual**: ปรับขนาดและตำแหน่งของพื้นที่วิเคราะห์ได้อย่างอิสระ
- **⚡ ตำแหน่งด่วน (Quick Positions)**: คลิกปุ่มเพื่อเลื่อน ROI ไปยังตำแหน่งต่างๆ ได้ทันที (บน/ล่าง/ซ้าย/ขวา/กลาง)
- **🖱️ Interactive ROI Selection**: คลิกเลือกตำแหน่ง ROI บนภาพได้โดยตรงด้วย Plotly
- **👁️ แสดงกรอบ ROI**: เห็นพื้นที่ที่กำลังวิเคราะห์บนภาพแบบ real-time
- **🤖 เทรนโมเดล**: เทรนโมเดล Machine Learning จากข้อมูลที่รวบรวม
- **📈 Calibration Curve**: แสดงกราฟความสัมพันธ์ระหว่างค่าจริงและค่าทำนาย พร้อม R² score
- **🔬 LOD Calculation**: คำนวณ Limit of Detection จากตัวอย่าง blank อัตโนมัติ
- **📋 Data Management**: ดู แก้ไข และลบข้อมูลในแต่ละ profile ได้สะดวก
- **🔮 ทำนายผล**: ทำนายค่าความเข้มข้นจากรูปภาพใหม่
### ความต้องการของระบบ

- Python 3.8 หรือสูงกว่า
- pip (Python package manager)

### ขั้นตอนการติดตั้ง

1. **Clone หรือดาวน์โหลดโปรเจกต์**

```bash
git clone <repository-url>
cd colorimetric-analysis-app
```

2. **สร้าง Virtual Environment (แนะนำ)**

```bash
# สร้าง virtual environment
python -m venv venv

# เปิดใช้งาน virtual environment
# สำหรับ macOS/Linux:
source venv/bin/activate

# สำหรับ Windows:
venv\Scripts\activate
```

3. **ติดตั้ง Dependencies**

```bash
pip install -r requirements.txt
```

## 📖 การใช้งาน

### เริ่มต้นใช้งานแอปพลิเคชัน

รันคำสั่งต่อไปนี้เพื่อเริ่มแอปพลิเคชัน:

```bash
streamlit run app.py
```

แอปพลิเคชันจะเปิดในเบราว์เซอร์ที่ `http://localhost:8501`

### ขั้นตอนการใช้งาน

#### 0. จัดการ Profile สาร (ขั้นตอนแรก)

**เลือก Profile:**
1. ดูที่ **Sidebar ด้านซ้าย**
2. เลือก Profile สารที่ต้องการวิเคราะห์จาก dropdown (Phosphate, Nitrate, Ammonia)

**เพิ่ม Profile ใหม่:**
1. คลิก "➕ เพิ่ม Profile ใหม่" ใน Sidebar
2. กรอกชื่อสาร (เช่น Iron, Copper)
3. เลือกหน่วยความเข้มข้น (mg/L, ppm, µg/L, %, g/L, mol/L)
4. คลิก "เพิ่ม Profile"

**ลบ Profile:**
1. คลิก "🗑️ ลบ Profile" ใน Sidebar
2. เลือก Profile ที่ต้องการลบ
3. คลิก "ลบ Profile" (ไฟล์ dataset และ model จะไม่ถูกลบ)

**ปรับหน่วยความเข้มข้น:**
1. คลิก "📏 ปรับหน่วยความเข้มข้น" ใน Sidebar
2. เลือกหน่วยใหม่สำหรับ Profile ปัจจุบัน
3. คลิก "บันทึกหน่วย"

**หมายเหตุ:** แต่ละ Profile จะมีไฟล์:
- Dataset: `{profile}_dataset.csv` (เช่น `phosphate_dataset.csv`)
- Model: `{profile}_model.joblib` (เช่น `phosphate_model.joblib`)
- หน่วยความเข้มข้นแยกตาม Profile

#### 1. รวบรวมข้อมูล (Data Collection)

1. ไปที่แท็บ **"📊 รวบรวมข้อมูล"**
2. **ตั้งค่า ROI** (Region of Interest):
   - **ขนาด ROI**: ปรับขนาดพื้นที่วิเคราะห์ (20-300 พิกเซล, ค่าเริ่มต้น: 100x100)
   - **ตำแหน่ง ROI**: เลือก "กลางภาพ", "กำหนดเอง", หรือ "คลิกเลือก (Interactive)"
   - หากเลือก "กำหนดเอง" สามารถปรับตำแหน่ง X, Y ได้ตามต้องการ
     - เลื่อนแนวนอน (X): ซ้าย (-) หรือขวา (+)
     - เลื่อนแนวตั้ง (Y): บน (-) หรือล่าง (+)
   - หากเลือก "คลิกเลือก (Interactive)" สามารถคลิกบนภาพเพื่อเลือกตำแหน่ง ROI ได้โดยตรง
3. คลิก **"Browse files"** เพื่ออัปโหลดรูปภาพ (รองรับ PNG, JPG, JPEG)
4. **ดูกรอบ ROI บนภาพ**: ระบบจะแสดงกรอบสีเขียวบนภาพเพื่อให้เห็นพื้นที่ที่กำลังวิเคราะห์
5. ระบบจะคำนวณค่า RGB เฉลี่ยจากพื้นที่ ROI ที่กำหนด
6. กรอก **ค่าความเข้มข้น** (mg/L) ที่วัดได้จริง (ใช้ 0 สำหรับตัวอย่าง blank)
7. คลิก **"💾 บันทึกข้อมูล"** เพื่อบันทึกลงไฟล์ `dataset.csv`
8. ทำซ้ำขั้นตอนนี้กับรูปภาพหลายๆ ภาพ (แนะนำอย่างน้อย 10-20 ตัวอย่าง)

**หมายเหตุ:** ควรใช้การตั้งค่า ROI เดียวกัน (ทั้งขนาดและตำแหน่ง) สำหรับทุกตัวอย่างเพื่อความสม่ำเสมอ

#### 2. เทรนโมเดล (Model Training)

1. ไปที่แท็บ **"🤖 เทรนโมเดล"**
2. **จัดการข้อมูล** (ถ้าต้องการ):
   - คลิก "📋 จัดการข้อมูล" เพื่อดูตารางข้อมูลทั้งหมด
   - ตารางจะแสดง: ลำดับ | Image (ชื่อไฟล์รูป) | R | G | B | Concentration
   - สามารถเลือกลบข้อมูลที่ไม่ต้องการได้โดยเลือกลำดับแล้วคลิก "ลบข้อมูลที่เลือก"
   - ดูสถิติข้อมูล: จำนวนตัวอย่าง, ความเข้มข้นต่ำสุด/สูงสุด/เฉลี่ย
3. คลิก **"🚀 เริ่มเทรนโมเดล"**
4. รอให้ระบบเทรนโมเดล (ใช้เวลาไม่กี่วินาที)
5. **ดูผลลัพธ์:**
   - **R² Score**: ความแม่นยำของโมเดล (0-1)
   - **LOD (Limit of Detection)**: ค่าความเข้มข้นต่ำสุดที่วัดได้ (ต้องมี blank ≥ 3 ตัวอย่าง)
   - **Calibration Curve**: กราฟแสดงความสัมพันธ์ระหว่างค่าจริง vs ค่าทำนาย
6. โมเดลจะถูกบันทึกเป็นไฟล์ `{profile}_model.joblib`

**การตีความ R² Score:**
- **≥ 0.9**: ความแม่นยำสูงมาก 🎯
- **0.7-0.9**: ความแม่นยำดี 👍
- **0.5-0.7**: ความแม่นยำปานกลาง ⚠️
- **< 0.5**: ความแม่นยำต่ำ ❌ (ควรรวบรวมข้อมูลเพิ่ม)

**การตีความ Calibration Curve:**
- จุดที่อยู่ใกล้เส้นแดง (y=x) = โมเดลทำนายได้แม่นยำ
- จุดที่กระจายออกจากเส้น = โมเดลทำนายคลาดเคลื่อน
- R² score บนกราฟแสดงความแม่นยำโดยรวม

**LOD (Limit of Detection):**
- คำนวณจาก: LOD = mean(blank predictions) + 3×SD(blank predictions)
- ต้องมีตัวอย่าง blank (concentration = 0) อย่างน้อย 3 ตัวอย่าง
- แสดงค่าความเข้มข้นต่ำสุดที่สามารถตรวจวัดได้อย่างน่าเชื่อถือ

#### 3. ทำนายผล (Prediction)

1. ไปที่แท็บ **"🔮 ทำนายผล"**
2. **ตั้งค่า ROI** ให้ตรงกับที่ใช้ตอนรวบรวมข้อมูล:
   - ปรับขนาด ROI ให้เท่ากับที่ใช้ตอนเทรน
   - เลือกตำแหน่ง ROI (กลางภาพหรือกำหนดเอง)
   - หากใช้ตำแหน่งกำหนดเอง ปรับ X, Y ให้ตรงกับที่ใช้ตอนเทรน
3. อัปโหลดรูปภาพใหม่ที่ต้องการทำนายค่าความเข้มข้น
4. **ตรวจสอบกรอบ ROI**: ดูกรอบสีเขียวบนภาพเพื่อยืนยันว่ากำลังวิเคราะห์พื้นที่ที่ถูกต้อง
5. ระบบจะแสดงค่า RGB ที่วิเคราะห์ได้
6. ระบบจะแสดงค่าความเข้มข้นที่ทำนายได้ (mg/L)

**หมายเหตุ:** ควรใช้การตั้งค่า ROI เดียวกันทุกประการกับที่ใช้ตอนรวบรวมข้อมูลเพื่อความแม่นยำสูงสุด

## 📁 โครงสร้างไฟล์

```
colorimetric-analysis-app/
├── app.py                      # แอปพลิเคชันหลัก Streamlit
├── test_app.py                # Unit tests
├── requirements.txt           # Python dependencies
├── README.md                  # เอกสารนี้
├── phosphate_dataset.csv      # ไฟล์ข้อมูล Phosphate (สร้างอัตโนมัติ)
├── phosphate_model.joblib     # โมเดล ML Phosphate (สร้างอัตโนมัติ)
├── nitrate_dataset.csv        # ไฟล์ข้อมูล Nitrate (สร้างอัตโนมัติ)
├── nitrate_model.joblib       # โมเดล ML Nitrate (สร้างอัตโนมัติ)
└── ...                        # ไฟล์ profile อื่นๆ
```

**หมายเหตุ:** แต่ละ profile จะสร้างไฟล์ dataset และ model แยกกัน

## 🧪 การทดสอบ

รัน unit tests ด้วยคำสั่ง:

```bash
# ใช้ unittest
python -m unittest test_app.py

# หรือใช้ pytest
pytest test_app.py -v
```

## 🔧 เทคโนโลยีที่ใช้

- **Streamlit**: Web framework สำหรับสร้าง UI
- **OpenCV**: ประมวลผลภาพและคำนวณค่า RGB
- **Pandas**: จัดการข้อมูลในรูปแบบ DataFrame
- **Scikit-learn**: เทรนโมเดล Machine Learning (RandomForestRegressor) และคำนวณ metrics
- **Matplotlib**: สร้างกราฟ Calibration Curve
- **Plotly**: Interactive ROI selection และ visualization
- **Joblib**: บันทึกและโหลดโมเดล
- **PIL/Pillow**: แสดงภาพใน UI
- **NumPy**: คำนวณทางคณิตศาสตร์และสถิติ

## 📊 วิธีการทำงาน

### การประมวลผลภาพ

1. อ่านภาพด้วย OpenCV
2. แปลงจาก BGR เป็น RGB color space
3. คำนวณตำแหน่ง ROI:
   - หาจุดกึ่งกลางของภาพ
   - ปรับตำแหน่งตาม offset ที่กำหนด (X, Y)
4. ตัดเอา Region of Interest (ROI) จากตำแหน่งที่คำนวณได้
   - **ขนาด ROI**: ปรับได้ตั้งแต่ 20x20 ถึง 300x300 พิกเซล (ค่าเริ่มต้น: 100x100)
   - **ตำแหน่ง ROI**: กลางภาพหรือกำหนดเองด้วย X/Y offset
5. **แสดง ROI บนภาพ**: วาดกรอบสีเขียวบนภาพเพื่อแสดงพื้นที่ที่กำลังวิเคราะห์
6. คำนวณค่าเฉลี่ยของแต่ละช่องสี (R, G, B) จากพื้นที่ ROI

**การแสดงผล ROI:**
- กรอบสีเขียว = พื้นที่ที่ใช้คำนวณค่า RGB
- แสดงขนาด ROI บนภาพ (เช่น "ROI: 100x100px")
- อัปเดตแบบ real-time เมื่อปรับการตั้งค่า

**หมายเหตุ**: 
- ถ้าภาพมีขนาดเล็กกว่า ROI ที่กำหนด ระบบจะใช้ภาพทั้งหมดในการคำนวณ
- ควรใช้การตั้งค่า ROI เดียวกัน (ขนาดและตำแหน่ง) สำหรับทุกตัวอย่างเพื่อความสม่ำเสมอ
- ฟีเจอร์ปรับตำแหน่ง ROI มีประโยชน์เมื่อตัวอย่างไม่ได้อยู่ตรงกลางภาพ
- การแสดงกรอบ ROI ช่วยให้มั่นใจว่ากำลังวิเคราะห์พื้นที่ที่ถูกต้อง

### โมเดล Machine Learning

- **Algorithm**: Random Forest Regressor
- **Features**: ค่า R, G, B (3 features)
- **Target**: ค่าความเข้มข้น (Concentration)
- **Hyperparameters**: 
  - `n_estimators=100` (จำนวน decision trees)
  - `random_state=42` (สำหรับ reproducibility)

## ⚠️ ข้อควรระวัง

1. **คุณภาพของข้อมูล**: ควรรวบรวมข้อมูลอย่างน้อย 10-20 ตัวอย่างเพื่อความแม่นยำที่ดี
2. **การถ่ายภาพ**: ควรถ่ายภาพในสภาพแสงที่คงที่และวางตัวอย่างไว้ตรงกลางภาพ
3. **ช่วงความเข้มข้น**: โมเดลจะทำนายได้ดีในช่วงความเข้มข้นที่ใกล้เคียงกับข้อมูลที่ใช้เทรน
4. **การเทรนใหม่**: ถ้าเพิ่มข้อมูลใหม่ ควรเทรนโมเดลใหม่เพื่อความแม่นยำที่ดีขึ้น

## 🐛 การแก้ไขปัญหา

### ปัญหา: ไม่สามารถอัปโหลดรูปภาพได้

- ตรวจสอบว่าไฟล์เป็น PNG, JPG หรือ JPEG
- ตรวจสอบขนาดไฟล์ไม่ใหญ่เกินไป (Streamlit มีขีดจำกัดที่ 200MB)

### ปัญหา: โมเดลทำนายไม่แม่นยำ

- รวบรวมข้อมูลเพิ่มเติม (อย่างน้อย 20-30 ตัวอย่าง)
- ตรวจสอบว่าข้อมูลครอบคลุมช่วงความเข้มข้นที่ต้องการทำนาย
- ถ่ายภาพในสภาพแสงที่คงที่

### ปัญหา: ไม่พบโมเดล

- ไปที่แท็บ "เทรนโมเดล" และเทรนโมเดลก่อน
- ตรวจสอบว่ามีไฟล์ `model.joblib` ในโฟลเดอร์โปรเจกต์

### ปัญหา: ข้อมูลไม่เพียงพอ

- ต้องมีข้อมูลอย่างน้อย 5 ตัวอย่างก่อนเทรนโมเดล
- แนะนำให้มี 10-20 ตัวอย่างขึ้นไปเพื่อความแม่นยำที่ดี

## 📝 License

This project is open source and available for educational and research purposes.

## 👥 การสนับสนุน

หากพบปัญหาหรือมีข้อเสนอแนะ กรุณาติดต่อผู้พัฒนาหรือสร้าง issue ใน repository

## 🔄 การพัฒนาต่อ

แนวทางการพัฒนาในอนาคต:
- ✅ ~~เพิ่มการปรับขนาด ROI แบบ manual~~ (เสร็จแล้ว)
- ✅ ~~เพิ่มการปรับตำแหน่ง ROI แบบ manual (X/Y offset)~~ (เสร็จแล้ว)
- ✅ ~~แสดง ROI overlay บนภาพเพื่อให้เห็นพื้นที่ที่กำลังวิเคราะห์~~ (เสร็จแล้ว)
- ✅ ~~รองรับ Multi-Profile สำหรับวิเคราะห์สารหลายชนิด~~ (เสร็จแล้ว)
- ✅ ~~เพิ่มการจัดการ Profile (เพิ่ม, ลบ, ปรับหน่วย)~~ (เสร็จแล้ว)
- ✅ ~~แสดง Calibration Curve~~ (เสร็จแล้ว)
- ✅ ~~คำนวณ LOD (Limit of Detection)~~ (เสร็จแล้ว)
- ✅ ~~เพิ่มการจัดการข้อมูล (ดู, ลบ, สถิติ)~~ (เสร็จแล้ว)
- ✅ ~~บันทึกชื่อไฟล์รูปในข้อมูล~~ (เสร็จแล้ว)
- ✅ ~~เพิ่มการเลือกตำแหน่ง ROI แบบ interactive (คลิกเลือกบนภาพ)~~ (เสร็จแล้ว)
- รองรับโมเดล ML หลายแบบ (Linear Regression, SVM, Neural Network, etc.)
- Export ผลลัพธ์เป็น PDF report พร้อมกราฟและสถิติ
- รองรับการวิเคราะห์หลายตัวอย่างพร้อมกัน (batch processing)
- คำนวณ LOQ (Limit of Quantification)
- เพิ่ม Quality Control checks
